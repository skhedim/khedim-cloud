apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: guestbook-khedim
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: khedim.34.79.246.69.xip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: guestbook-khedim
            port:
              number: 80